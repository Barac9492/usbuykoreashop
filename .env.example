# Application
APP_NAME=first-project
NODE_ENV=development
BASE_URL=http://localhost:3000
BASE_URL_OTHER_PORT=

# Authentication & Security
ADMIN_PASSWORD=change-me-strong
JWT_SECRET=change-me-secret

# Providers
OPENROUTER_API_KEY=

# Database (optional; schema currently uses internal docker URL)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/app

# Notes:
# - Do NOT commit real secrets. Copy to `.env` and edit locally.
# - Docker compose reads from `.env` at project root.
